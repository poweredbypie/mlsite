import React from 'react'
import { Container } from 'react-bootstrap'

interface ListProps {
  name: string
  creator: string
  points: number
}

const List: React.FC<ListProps> = ({ name, creator, points }: ListProps) => {


  return (
    <>
    
    </>
  )
}

export default List